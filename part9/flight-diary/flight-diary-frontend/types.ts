export interface NewDiaryEntry {
  date: string;
  visibility: string;
  weather: string;
  comment: string;
}

export interface DiaryEntry {
  id: number;
  date: string;
  visibility: string;
  weather: string;
  comment: string;
}
